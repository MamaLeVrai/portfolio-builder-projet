package alt.portfolio.builder.services;

import org.springframework.stereotype.Service;

/**
 * Service pour la gestion des images (entité Image avec URL).
 *
 * Ce service est vide pour l'instant. Il est prévu pour gérer les images
 * associées aux profils via l'entité Image (relation ManyToMany avec Profile).
 *
 * Note : pour l'upload de fichiers (photos de profil, images de projets),
 * c'est ImageUploadService (Epic 5) qui s'en charge.
 */
@Service
public class ImageService {

}
