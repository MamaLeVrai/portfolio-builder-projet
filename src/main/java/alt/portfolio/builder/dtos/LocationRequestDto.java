package alt.portfolio.builder.dtos;

/**
 * DTO (Data Transfer Object) pour les requêtes de création/modification de lieu.
 *
 * Ce DTO est vide pour l'instant. Les lieux sont actuellement sélectionnés
 * par leur ID dans le formulaire d'ajout d'item (via locationId dans ItemController).
 *
 * Ce DTO pourrait être utilisé pour créer ou modifier des lieux directement
 * via un formulaire dédié dans une future version.
 */
public class LocationRequestDto {

}
