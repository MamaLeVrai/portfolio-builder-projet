package alt.portfolio.builder.dtos;

/**
 * DTO (Data Transfer Object) pour les requêtes de création/modification de catégorie.
 *
 * Un DTO est un objet qui sert à transporter des données entre le navigateur et le serveur.
 * Ce DTO est vide pour l'instant car les catégories ne sont pas gérées via formulaire.
 * Elles sont créées directement en base de données par un administrateur.
 */
public class CategoryRequestDto {

}
